import {useParams} from "react-router-dom";

const UpdatePlace = () => {
    const placeId = useParams().placeId
    return (
        <div>
            hello
        </div>
    )
};
export default UpdatePlace;